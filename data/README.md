## Data setup
- Place raw rooftop images in `data/raw/`
- Processed images go in `data/processed/`
- Train/val/test splits are defined in CSV files here
- Raw images are not tracked in GitHub (too large)